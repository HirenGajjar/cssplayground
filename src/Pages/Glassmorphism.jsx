import React from "react";

function Glassmorphism() {
  return <div>Glassmorphism</div>;
}

export default Glassmorphism;

/*
THis page should does following:
have a range input that is for glass opacity value from 0 to 1 
backdrop blur input that has value with 0px to 100px
saturation with range input
color with that with color input
and user can enter their own image in background to test


if user makes the any changes in any input then show them live on output

by default there is an image in center of the page on the top of that the div that creates the output based on the value from inputs 

all the input should be well design on the right side with modern right side dashboard type design



*/
